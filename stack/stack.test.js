const Stack = require('./stack');
let stack;
beforeEach(() => {
  stack = new Stack();
})

describe('a stack', () => {
  test('has a data value equal instance variable', () => {
    console.log(stack);
    expect(stack).toBe();
  });
})
